tasks:
  - before: sudo pip3 install ansible docker
    init: ansible-galaxy collection build && ansible-galaxy collection install community.*
# Ports to expose on workspace startup
ports:
  - port: 6080
    onOpen: open-preview
checkoutLocation: "/workspace/ansible_collections/community/proxysql"